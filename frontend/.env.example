# Production (Vercel) — set this in Vercel Dashboard → Settings → Environment Variables
VITE_API_URL=https://todo-app-production-5017.up.railway.app/api

# Local development — leave VITE_API_URL unset; Vite proxies /api → localhost:3004
