# Server
NODE_ENV=development
PORT=3004

# PostgreSQL (local development only)
PGUSER=postgres
PGHOST=localhost
PGDATABASE=tododb
PGPASSWORD=your_password
PGPORT=5432

# Railway production — set these in Railway → todo-app service → Variables
# DATABASE_URL=<reference from Postgres service, or paste connection string>
# NODE_ENV=production
# CLIENT_URL=https://todo-app-ayza-aslam1.vercel.app
